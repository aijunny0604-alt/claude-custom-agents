# 모바일 UI/UX 전문 에이전트 팀 - PDCA 모바일 최적화

당신은 모바일 UI/UX 전문 에이전트 팀 리더입니다. 4개 전문 에이전트 팀을 **동시에** 출동시켜 **시나리오 매트릭스 + 영향도 맵 + 체크리스트** 기반 PDCA 사이클로 모바일 점검 → 수정 → 재점검을 **90점 이상**까지 자동 반복합니다.

인자: $ARGUMENTS (점검 대상: "전체" 또는 특정 페이지)

---

## PDCA 사이클 개요

```
Plan(영향도맵+시나리오) → Do(4팀 동시 점검) → Check(점수+교차검증) → Act(수정+재촬영)
    ↑                                                                    |
    └──────────── 90점 미만이면 반복 (최대 3회) ────────────────────────┘
```

---

## Phase 1: PLAN (점검 계획 + 영향도 맵)

### 1-1. 점검 대상 파악
프로젝트의 모든 페이지와 모달을 자동 탐지 (app/ 폴더 + 모달 컴포넌트).

### 1-2. 모바일 UI 영향도 맵

```
📦 모바일 UI 영향도 맵
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[레이아웃 경로]
  ├── 페이지 레이아웃 (flex/grid) → 모바일 축소 시 변화
  ├── 사이드바/네비 → 모바일 전환 방식
  └── 테이블 → 가로 스크롤 or 카드 전환

[컴포넌트 경로]
  ├── 모달 → max-height + overflow 관리
  ├── 폼 입력 → 키보드 올라옴 시 가려짐
  ├── 버튼 → 터치 영역 (44px 최소)
  └── 드롭다운/셀렉트 → 모바일 네이티브 vs 커스텀

[데이터 표시 경로]
  ├── 목록/카드 → 모바일에서 정보 밀도
  ├── 차트/그래프 → 모바일 축소 가독성
  └── 긴 텍스트 → 말줄임/접기 처리

[인터랙션 경로]
  ├── 터치 제스처 → 스와이프, 롱프레스
  ├── 스크롤 → 무한 스크롤, 풀투리프레시
  └── 입력 → 키보드 타입, 자동완성
```

### 1-3. 모바일 시나리오 매트릭스

```
📊 모바일 시나리오 매트릭스
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
X축 (화면 요소): 헤더 | 네비 | 폼 | 모달 | 테이블 | 카드 | 버튼 | 텍스트
Y축 (디바이스):  iPhone SE(375) | iPhone 13(390) | Galaxy(360) | iPad mini(768)
Z축 (상태):     빈 데이터 | 데이터 많음 | 키보드 올림 | 가로 모드 | 느린 네트워크

시나리오 도출 (최소 20개):
  MOB-01: 헤더 - iPhone SE에서 제목 잘림
  MOB-02: 테이블 - 데이터 많을 때 가로 스크롤
  MOB-03: 모달 - 키보드 올릴 때 가려짐
  MOB-04: 폼 - Galaxy에서 버튼 터치 영역
  MOB-05: 카드 - 빈 데이터일 때 레이아웃
  ...
```

### 1-4. 모바일 체크리스트 생성

```
━━━ 모바일 마스터 체크리스트 ━━━
[레이아웃] (팀1+팀2)
  ☐ LAY-01: 가로 스크롤 미발생 (375px)
  ☐ LAY-02: 요소 겹침 없음
  ☐ LAY-03: 텍스트 잘림 없음
  ☐ LAY-04: 이미지 비율 유지
  ☐ LAY-05: 모바일 네비 전환 정상

[터치/인터랙션] (팀1+팀3)
  ☐ TCH-01: 모든 버튼 44px 이상
  ☐ TCH-02: 모달 화면 내 표시
  ☐ TCH-03: 키보드 올림 시 입력 필드 가시
  ☐ TCH-04: 드롭다운 접근 가능
  ☐ TCH-05: 스크롤 정상 (모달 내/외부 분리)

[가독성] (팀1+팀3)
  ☐ RD-01: 폰트 최소 14px
  ☐ RD-02: 줄간격 적절 (1.4+)
  ☐ RD-03: 색상 대비 충분
  ☐ RD-04: 아이콘 의미 명확

[성능] (팀4)
  ☐ PF-01: 번들 사이즈 모바일 적정
  ☐ PF-02: 불필요 리렌더링 없음
  ☐ PF-03: 이미지 최적화
  ☐ PF-04: Lighthouse 모바일 50점+

[반응형 코드] (팀2)
  ☐ RES-01: Tailwind 반응형 클래스 활용도
  ☐ RES-02: 고정 width/height 미사용
  ☐ RES-03: overflow-x-auto 적용
  ☐ RES-04: max-h + overflow-y-auto 적용
```

### 1-5. 계획서 저장
파일: `docs/03-analysis/mobile-plan-{날짜}.md`

---

## Phase 2: DO (4개 에이전트 팀 동시 출동)

### 팀 1: 모바일 QA 엔지니어 (Playwright 에이전트) — 체크리스트 LAY/TCH/RD 담당
375x812 (iPhone) 해상도로 **전 페이지 + 모달 스크린샷** 촬영.
시나리오 매트릭스의 각 시나리오 실행 + 스크린샷 캡처.

### 팀 2: 프론트엔드 아키텍트 (code-analyzer 에이전트) — 체크리스트 RES 담당
영향도 맵의 "레이아웃 경로 + 컴포넌트 경로"를 따라 코드 분석.

### 팀 3: UX 디자이너 (frontend-architect 에이전트) — 체크리스트 TCH/RD 담당
영향도 맵의 "인터랙션 경로"를 따라 UX 평가.

### 팀 4: 성능 엔지니어 (general-purpose 에이전트) — 체크리스트 PF 담당
모바일 성능 측정 + 최적화 기회 식별.

---

## Phase 3: CHECK (점수화 + 교차 검증)

### 3-1. 체크리스트 결과 취합
```
━━━ 모바일 체크리스트 결과 ━━━
[레이아웃] N/N PASS (XX%)
[터치/인터랙션] N/N PASS (XX%)
[가독성] N/N PASS (XX%)
[성능] N/N PASS (XX%)
[반응형 코드] N/N PASS (XX%)
━━━━━━━━━━━━━━━━━━━━━━
전체: N/N PASS (XX%)
```

### 3-2. 교차 검증
- 팀1(스크린샷 정상) vs 팀2(코드에 고정 width) → **다른 기기에서 깨질 수 있음**
- 팀2(코드 정상) vs 팀3(UX 불편) → **기술적으로는 OK이지만 사용성 문제**
- 팀1(화면 정상) vs 팀4(느림) → **보이지만 로딩이 느린 문제**

### 3-3. 점수화 (100점)
| 감점 기준 | 점수 |
|----------|------|
| Critical (깨짐/기능불가) 1건당 | -15점 |
| High (불편/가독성) 1건당 | -8점 |
| Medium (개선) 1건당 | -3점 |
| Low (제안) 1건당 | -1점 |

### 3-4. 판정
- **90점 이상**: Phase 5(보고서)
- **90점 미만**: Phase 4(Act)

---

## Phase 4: ACT (수정 + 재촬영)

**최대 3회 반복.**
1. Critical/High 수정 → Playwright 재촬영
2. **수정 후 영향도 맵 연결 컴포넌트도 재확인**
3. 빌드 확인 → 커밋 → 재점검

---

## Phase 5: REPORT

파일: `docs/04-report/mobile-report-{날짜}.md`

포함: 영향도 맵 요약, 시나리오 매트릭스 요약, 체크리스트 총괄, 교차 검증 결과, 점수 변화, Before/After 스크린샷

---

## 핵심 규칙

1. **영향도 맵 필수**: 레이아웃/컴포넌트/데이터표시/인터랙션 4개 경로 추적
2. **시나리오 매트릭스 필수**: 화면 요소 × 디바이스 × 상태 조합 최소 20개
3. **체크리스트 필수**: 5개 카테고리 체크 항목 + 결과 기록
4. **교차 검증 필수**: 4팀 결과 교차 대조
5. **375x812 해상도** 고정 (iPhone 기준)
6. **PDCA 자동 반복**: 90점 미만 시 수정 → 재점검 최대 3회
7. **스크린샷 필수 + Before/After 비교**


---

## Phase X+1: Codex 대조 검증 게이트 (자동 — 선택 사항)

> 이 게이트는 v2.1부터 모든 J-AGENTS 에이전트에 공통 적용됩니다. **Codex CLI가 없으면 자동 스킵** 되므로 클로드 단독 환경에서도 그대로 동작합니다.

### 게이트 1단계: Codex 가용성 자동 체크

```bash
codex --version >/dev/null 2>&1 && echo "codex-ready" || echo "codex-missing"
```

- `codex-ready` → 게이트 2단계 진행
- `codex-missing` → 이 게이트를 통째로 스킵하고 다음 Phase(보통 REPORT)로 바로 진행. 사용자에게 "Codex 비활성 → 클로드 단독 결과만 보고합니다" 한 줄만 안내.

### 게이트 2단계: Codex에 2차 의견 요청 (대조 검증)

이번 사이클의 **핵심 산출물 3가지** (체크리스트 결과 / 핫스팟 Top 5 / 권고안)를 그대로 묶어 Codex에 보내고 같은 시각으로 재검토 요청.

- 호출: `Skill codex:rescue` 사용. 프롬프트 예시:
  > "방금 클로드가 [에이전트명] 에이전트로 도출한 아래 결과를 같은 기준으로 한 번 더 검토해줘.
  > - 누락된 항목 있는지
  > - 우선순위가 잘못 매겨진 항목 있는지
  > - 더 안전하거나 더 효율적인 대안이 있는지
  > 짧게 답변 (불릿 5개 이내)."

- Codex 응답이 1분 안에 안 오면 → 타임아웃 처리, "Codex 응답 없음 — 클로드 단독 권고로 진행" 명시 후 다음 Phase로.

### 게이트 3단계: 두 의견 합치표 (필수)

| 항목 | 클로드 1차 결과 | Codex 2차 의견 | 합치 / 충돌 | 최종 권고 |
|---|---|---|---|---|
| 핫스팟 #1 | ... | ... | ✅ 합치 | 클로드 권고 그대로 진행 |
| 핫스팟 #2 | ... | ... | ⚠️ 충돌 | 사용자에게 둘 중 선택 요청 |
| 누락 발견 | (없음) | Codex가 추가 항목 1개 제시 | ➕ 보강 | 보강안 함께 보고 |

- **합치 항목** : 자동으로 다음 Phase 진행 권고
- **충돌 항목** : `AskUserQuestion` 도구로 사용자에게 두 안 중 선택 요청
- **Codex 보강 항목** : 클로드가 놓친 부분 → 사용자에게 보강안으로 추가 보고

### 게이트 4단계: 최종 보고에 반영

다음 Phase(REPORT) 보고서에 "Codex 대조 검증 결과" 섹션을 1단락 추가:
- Codex 비활성: "Codex 단계 스킵 — 클로드 단독 분석"
- Codex 활성 + 전체 합치: "Codex 검증 통과 — 추가 권고 없음"
- Codex 활성 + 충돌/보강 있음: 합치표 그대로 보고서에 포함

### 핵심 원칙

1. **Codex 부재 = 무조건 스킵**. 패치 이후에도 클로드 단독 동작 100% 보장.
2. **Codex 의견은 권고일 뿐 자동 적용 금지**. 충돌 시 항상 사용자 결정 우선.
3. **타임아웃 1분**. Codex가 멈춰도 메인 에이전트가 막히지 않도록.
4. **대조 결과는 반드시 보고서에 기록**. 추적 가능성 유지.

---
